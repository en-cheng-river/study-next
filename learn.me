# 动态路由学习
如果都在同一级使用动态路由，eg：[name]和[age],会报错，提示信息是：
    Failed to reload dynamic routes: Error: You cannot use different slug names for the same dynamic path ('name' !== 'age').
    重新加载动态路由失败：错误：不能对同一动态路径使用不同的段塞名称（'name'！=='age'）。
所以好的解决办法是，分别创建一个普通路由文件夹，然后再创建动态路由名：
- name
    - [name]
        - page.tsx

- age
    - [age]
        - page.tsx